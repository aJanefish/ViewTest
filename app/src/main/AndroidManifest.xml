<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.viewtest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.AppCompat">
        <activity android:name=".activity.ChildErrorActivity"></activity>
        <activity
            android:name=".activity.TestMotionEventCActivity"
            android:label="TestMotionEventCActivity" />
        <activity
            android:name=".activity.TestMotionEventBActivity"
            android:label="TestMotionEventBActivity" />
        <activity
            android:name=".activity.TestMotionEventAActivity"
            android:label="TestMotionEventAActivity" />
        <activity android:name=".activity.TouchDelegateActivity" />
        <activity android:name=".activity.ViewActivity" />
        <activity android:name=".activity.DialogActivity" />
        <activity android:name=".activity.WindowActivity" />
        <activity
            android:name=".activity.LocalVisibleRectActivity"
            android:theme="@style/DiyAppTheme" />
        <activity
            android:name=".activity.FrameOfReferenceActivity"
            android:theme="@style/DiyAppTheme" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>